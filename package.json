{
  "name": "allshield-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node tests/static-check.mjs && node --check app.js && node --check backend.js && node --check team-accounts.js && node --check live-platform.js && node --check ops-platform.js && node --check recruiting-platform.js"
  }
}
