name: Certify Careers Video Experience

on:
  push:
    branches: [main]
    paths:
      - 'careers-video-experience-fix-2026-08-27.js'
      - 'config.js'
      - 'tests/careers-video-experience-cert.mjs'
      - '.github/workflows/careers-video-experience-cert.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Certify Careers video experience on live production
        run: ALLSHIELD_LIVE_URL='https://allshieldinsurancegroup.com' node tests/careers-video-experience-cert.mjs
