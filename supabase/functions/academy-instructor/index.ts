import { createClient } from "npm:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST,OPTIONS"};
const json=(data:unknown,status=200)=>new Response(JSON.stringify(data),{status,headers:{...cors,"Content-Type":"application/json"}});
const TYPES=new Set(["study","listen","lesson_quiz","chapter_exam","state_exam"]);const MODES=new Set(["read","listen"]);const stateCode=(v:unknown)=>String(v||"").trim().toUpperCase().slice(0,2)||null;
const FACULTY:any={
 ava:{name:"Ava",style:"Warm & encouraging",voice_id:"330290724a1b470fb63153f34d4c0183",avatar_id:"ee45a16b921b442ba3275621d963bb31",image_url:"https://resource2.heygen.ai/public-avatars/Saoirse/paos/angles/office78_p3_a0.jpg",sample_audio_url:"https://resource2.heygen.ai/text_to_speech/63a514fdb9a2407fa74f0da0338d73bd/330290724a1b470fb63153f34d4c0183/id=fa1c300e-4db9-4ec0-b1b8-11f8ff24b54c.wav",welcome_video_id:"927c55ec45e65c0b372cf960d84f37f8",guided_voice:"marin"},
 maya:{name:"Maya",style:"Calm & clear",voice_id:"06672207805f41a9ad0af6797f8aa14b",avatar_id:"81708c00d9824a17a0a88c5666d2c2ac",image_url:"https://resource2.heygen.ai/public-avatars/Liza/paos/angles/office84_p1_a0.jpg",sample_audio_url:"https://resource2.heygen.ai/text_to_speech/63a514fdb9a2407fa74f0da0338d73bd/06672207805f41a9ad0af6797f8aa14b/id=3c30779d-aa52-4b64-b30c-05d50a45e7fd.wav",welcome_video_id:"2fda9014b66f4e5a348039d1787dc301",guided_voice:"coral"},
 jordan:{name:"Jordan",style:"Patient & reassuring",voice_id:"03fcf8ecb0a94b6b94e9007edb7c35f8",avatar_id:"f59cc9c022094549a49f09b33159c4eb",image_url:"https://resource2.heygen.ai/public-avatars/Dashiell/paos/angles/office84_p1_a1.jpg",sample_audio_url:"https://resource2.heygen.ai/text_to_speech/63a514fdb9a2407fa74f0da0338d73bd/03fcf8ecb0a94b6b94e9007edb7c35f8/id=69f26754-0ba8-4e6f-b614-82438e8a93c0.wav",welcome_video_id:"3908dc59dc03e17551fda9fa47d62e41",guided_voice:"cedar"},
 marcus:{name:"Marcus",style:"Direct & professional",voice_id:"88bb9ee1c81b466eb2a08fdde86d3619",avatar_id:"94aa5a1f0e39427da9a213f65f793caa",image_url:"https://resource2.heygen.ai/public-avatars/Sebastian/paos/angles/office86_p3_a1.jpg",sample_audio_url:"https://resource2.heygen.ai/text_to_speech/63a514fdb9a2407fa74f0da0338d73bd/88bb9ee1c81b466eb2a08fdde86d3619/id=80829f8b-f861-4c9d-b4b0-c13a22bc94f8.wav",welcome_video_id:"f56628df51e438bec69b62e13dc752b9",guided_voice:"onyx"}
};
const hex=async(input:string)=>{const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(input));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")};
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});if(req.method!=="POST")return json({error:"Method not allowed"},405);try{const ah=req.headers.get("Authorization")||"";if(!ah.startsWith("Bearer "))return json({error:"Missing authorization"},401);const token=ah.slice(7);const url=Deno.env.get("SUPABASE_URL")!;const pub=JSON.parse(Deno.env.get("SUPABASE_PUBLISHABLE_KEYS")||"{}").default||Deno.env.get("SUPABASE_ANON_KEY")!;const sec=JSON.parse(Deno.env.get("SUPABASE_SECRET_KEYS")||"{}").default||Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;const uc=createClient(url,pub,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{persistSession:false}});const {data:ud,error:ue}=await uc.auth.getUser(token);if(ue||!ud.user)return json({error:"Invalid session"},401);const admin=createClient(url,sec,{auth:{persistSession:false,autoRefreshToken:false}});const userId=ud.user.id;const {data:profile}=await admin.from("profiles").select("id,role,status,resident_state").eq("id",userId).single();if(!profile||["inactive","terminated"].includes(String(profile.status)))return json({error:"Academy access unavailable"},403);const body=await req.json();const action=String(body.action||"");
async function lessonAccess(lessonId:string){const {data:l,error:le}=await admin.from("academy_lessons").select("id,chapter_id,title,content").eq("id",lessonId).single();if(le||!l)throw new Error("Lesson not found");const {data:ch,error:ce}=await admin.from("academy_chapters").select("id,course_id,title").eq("id",l.chapter_id).single();if(ce||!ch)throw new Error("Chapter not found");const {data:c,error:coe}=await admin.from("courses").select("id,state_code,title").eq("id",ch.course_id).single();if(coe||!c)throw new Error("Course not found");const {data:a}=await admin.from("course_assignments").select("id").eq("user_id",userId).eq("course_id",c.id).maybeSingle();if(!a)throw new Error("Course not assigned");return{lesson:l,chapter:ch,course:c};}
async function chapterAccess(chapterId:string){const {data:ch,error:ce}=await admin.from("academy_chapters").select("id,course_id").eq("id",chapterId).single();if(ce||!ch)throw new Error("Chapter not found");const {data:c,error:coe}=await admin.from("courses").select("id,state_code").eq("id",ch.course_id).single();if(coe||!c)throw new Error("Course not found");const {data:a}=await admin.from("course_assignments").select("id").eq("user_id",userId).eq("course_id",c.id).maybeSingle();if(!a)throw new Error("Course not assigned");return{chapter:ch,course:c};}
async function pref(){const {data:p,error}=await admin.from("academy_instructor_preferences").select("instructor_key,voice_id,avatar_id,guided_enabled,guided_voice,guided_speed,introduction_seen_at,updated_at").eq("user_id",userId).maybeSingle();if(error)throw error;return p||null;}
if(action==="faculty"){const p=await pref();return json({ok:true,faculty:FACULTY,preference:p,professional_audio_ready:!!Deno.env.get("OPENAI_API_KEY")});}
if(action==="set_instructor"){const key=String(body.instructor_key||"").toLowerCase();const f=FACULTY[key];if(!f)return json({error:"Choose a valid instructor"},400);const prior=await pref();const row={user_id:userId,instructor_key:key,voice_id:f.voice_id,avatar_id:f.avatar_id,guided_voice:f.guided_voice,guided_enabled:prior?.guided_enabled??false,guided_speed:Number(prior?.guided_speed||1),updated_at:new Date().toISOString()};const {error}=await admin.from("academy_instructor_preferences").upsert(row,{onConflict:"user_id"});if(error)return json({error:error.message},400);return json({ok:true,preference:row,instructor:f});}
if(action==="set_guided"){const prior=await pref();const key=String(prior?.instructor_key||"ava");const f=FACULTY[key]||FACULTY.ava;const enabled=!!body.enabled;const speed=Math.max(.75,Math.min(1.5,Number(body.speed||prior?.guided_speed||1)));const row={user_id:userId,instructor_key:key,voice_id:prior?.voice_id||f.voice_id,avatar_id:prior?.avatar_id||f.avatar_id,guided_voice:String(prior?.guided_voice||f.guided_voice||"marin"),guided_enabled:enabled,guided_speed:speed,updated_at:new Date().toISOString()};const {error}=await admin.from("academy_instructor_preferences").upsert(row,{onConflict:"user_id"});if(error)return json({error:error.message},400);return json({ok:true,preference:row});}
if(action==="mark_introduction_seen"){const prior=await pref();const f=FACULTY[String(prior?.instructor_key||"ava")]||FACULTY.ava;const row={user_id:userId,instructor_key:String(prior?.instructor_key||"ava"),voice_id:prior?.voice_id||f.voice_id,avatar_id:prior?.avatar_id||f.avatar_id,guided_voice:prior?.guided_voice||f.guided_voice,guided_enabled:prior?.guided_enabled??false,guided_speed:Number(prior?.guided_speed||1),introduction_seen_at:new Date().toISOString(),updated_at:new Date().toISOString()};const {error}=await admin.from("academy_instructor_preferences").upsert(row,{onConflict:"user_id"});if(error)return json({error:error.message},400);return json({ok:true,preference:row});}
if(action==="guided_audio"){const lessonId=String(body.lesson_id||"");const text=String(body.text||"").trim();if(!lessonId||!text)return json({error:"Lesson and text are required"},400);await lessonAccess(lessonId);if(text.length>3900)return json({error:"Guided section is too long; split it into smaller sections."},400);const p=await pref();const voice=String(p?.guided_voice||"marin");const speed=Math.max(.75,Math.min(1.5,Number(body.speed||p?.guided_speed||1)));const apiKey=Deno.env.get("OPENAI_API_KEY");if(!apiKey)return json({error:"Professional guided narration is not configured."},503);const hash=await hex(`${voice}|${speed}|${text}`);const path=`guided-audio/${lessonId}/${hash}.mp3`;let signed=await admin.storage.from("academy-media").createSignedUrl(path,60*60*12);if(signed.data?.signedUrl)return json({ok:true,audio_url:signed.data.signedUrl,cached:true,voice,speed});const speech=await fetch("https://api.openai.com/v1/audio/speech",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-4o-mini-tts",voice,input:text,instructions:"Speak like an experienced professional insurance instructor in a calm, warm classroom tone. Sound human and conversational, not promotional or robotic. Use natural pauses for headings, definitions, examples, and exam tips. Pronounce ALLSHIELD as All Shield.",response_format:"mp3",speed})});if(!speech.ok){let msg=`Voice provider error ${speech.status}`;try{const e=await speech.json();msg=e?.error?.message||msg}catch{};return json({error:msg},502);}const audio=new Uint8Array(await speech.arrayBuffer());const {error:up}=await admin.storage.from("academy-media").upload(path,audio,{contentType:"audio/mpeg",upsert:true,cacheControl:"31536000"});if(up)return json({error:up.message},500);signed=await admin.storage.from("academy-media").createSignedUrl(path,60*60*12);if(!signed.data?.signedUrl)return json({error:"Guided audio was created but could not be opened."},500);return json({ok:true,audio_url:signed.data.signedUrl,cached:false,voice,speed});}
if(action==="get_resume"){const lessonId=String(body.lesson_id||"");if(!lessonId)return json({error:"Missing lesson id"},400);await lessonAccess(lessonId);const {data:pr,error}=await admin.from("academy_lesson_progress").select("resume_percent,last_mode,active_seconds,last_opened_at").eq("user_id",userId).eq("lesson_id",lessonId).maybeSingle();if(error)return json({error:error.message},400);return json({ok:true,resume_percent:Number(pr?.resume_percent||0),mode:String(pr?.last_mode||"read"),active_seconds:Number(pr?.active_seconds||0),last_opened_at:pr?.last_opened_at||null});}
if(action==="save_resume"){const lessonId=String(body.lesson_id||"");if(!lessonId)return json({error:"Missing lesson id"},400);await lessonAccess(lessonId);const resume=Math.max(0,Math.min(100,Number(body.resume_percent||0)));const mode=MODES.has(String(body.mode||"read"))?String(body.mode||"read"):"read";const now=new Date().toISOString();const {data:pr}=await admin.from("academy_lesson_progress").select("id").eq("user_id",userId).eq("lesson_id",lessonId).maybeSingle();if(pr){const {error}=await admin.from("academy_lesson_progress").update({resume_percent:resume,last_mode:mode,last_opened_at:now,updated_at:now}).eq("id",pr.id);if(error)return json({error:error.message},400);}else{const {error}=await admin.from("academy_lesson_progress").insert({user_id:userId,lesson_id:lessonId,status:"in_progress",active_seconds:0,resume_percent:resume,last_mode:mode,started_at:now,last_opened_at:now,updated_at:now});if(error)return json({error:error.message},400);}return json({ok:true,resume_percent:resume,mode});}
if(action==="record_activity"){const type=String(body.activity_type||"");if(!TYPES.has(type))return json({error:"Invalid activity type"},400);const seconds=Math.max(1,Math.min(60,Math.round(Number(body.seconds||0))));if(!Number.isFinite(seconds))return json({error:"Invalid seconds"},400);let lessonId=body.lesson_id?String(body.lesson_id):null;let chapterId=body.chapter_id?String(body.chapter_id):null;let examSessionId=body.exam_session_id?String(body.exam_session_id):null;let sc=stateCode(body.state_code||profile.resident_state);let contextKey="";if(lessonId){const x=await lessonAccess(lessonId);chapterId=String(x.chapter.id);sc=stateCode(x.course.state_code);contextKey=`lesson:${lessonId}`;}else if(chapterId){const x=await chapterAccess(chapterId);sc=stateCode(x.course.state_code);contextKey=`chapter:${chapterId}`;}else if(examSessionId){const {data:ex}=await admin.from("exam_sessions").select("id,state_code").eq("id",examSessionId).eq("user_id",userId).maybeSingle();if(!ex)return json({error:"Exam session not found"},404);sc=stateCode(ex.state_code);contextKey=`exam:${examSessionId}`;}else contextKey=`state:${sc||"NA"}`;const {data:row,error:re}=await admin.from("academy_activity_totals").select("id,active_seconds").eq("user_id",userId).eq("context_key",contextKey).eq("activity_type",type).maybeSingle();if(re)return json({error:re.message},400);const now=new Date().toISOString();const next=Math.min(604800,Number(row?.active_seconds||0)+seconds);if(row){const {error}=await admin.from("academy_activity_totals").update({active_seconds:next,last_active_at:now,state_code:sc,lesson_id:lessonId,chapter_id:chapterId,exam_session_id:examSessionId}).eq("id",row.id);if(error)return json({error:error.message},400);}else{const {error}=await admin.from("academy_activity_totals").insert({user_id:userId,context_key:contextKey,activity_type:type,state_code:sc,lesson_id:lessonId,chapter_id:chapterId,exam_session_id:examSessionId,active_seconds:next,started_at:now,last_active_at:now});if(error)return json({error:error.message},400);}return json({ok:true,activity_type:type,active_seconds:next});}
if(action==="summary"){const {data,error}=await admin.from("academy_activity_totals").select("activity_type,state_code,active_seconds,last_active_at").eq("user_id",userId);if(error)return json({error:error.message},400);const totals:any={study:0,listen:0,lesson_quiz:0,chapter_exam:0,state_exam:0};for(const r of data||[])totals[String(r.activity_type)]=(totals[String(r.activity_type)]||0)+Number(r.active_seconds||0);const examDerived=await admin.from("exam_sessions").select("started_at,completed_at").eq("user_id",userId).not("completed_at","is",null);let derived=0;for(const e of examDerived.data||[]){derived+=Math.max(0,Math.round((new Date(e.completed_at).getTime()-new Date(e.started_at).getTime())/1000));}totals.state_exam=Math.max(totals.state_exam,derived);const totalSeconds=totals.study+totals.lesson_quiz+totals.chapter_exam+totals.state_exam;return json({ok:true,totals,total_seconds:totalSeconds,listen_seconds:totals.listen});}
return json({error:"Unknown action"},400);}catch(e){return json({error:e instanceof Error?e.message:String(e)},500);}});
